<?xml version="1.0" encoding="utf-8"?>
<!-- メニュー項目(カード)の背景です。
     maskを指定することでrippleが角丸の内側に収まります。 -->
<ripple
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/menuItemRipple">
  <item android:id="@android:id/mask">
    <shape android:shape="rectangle">
      <solid android:color="#FFFFFFFF" />
      <corners android:radius="@dimen/menu_item_corner_radius" />
    </shape>
  </item>
  <item>
    <shape android:shape="rectangle">
      <solid android:color="@color/menuItemBackground" />
      <corners android:radius="@dimen/menu_item_corner_radius" />
      <stroke
          android:width="1dp"
          android:color="@color/menuItemStroke" />
    </shape>
  </item>
</ripple>
