<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/AppLayout">
  <Button
      android:id="@+id/rc1_reader_button"
      android:text="@string/rc1_reader"
      style="@style/MenuButton" />
  <Button
      android:id="@+id/rc2_reader_button"
      android:text="@string/rc2_reader"
      style="@style/MenuButton" />
</LinearLayout>
